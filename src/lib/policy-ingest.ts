/**
 * Policy document ingest — rename, file, attach schedule from carrier library.
 * Coterie v1 matches against the checked-in form library (no fake OCR claims).
 */

export { fileDocument, attachCoterieScheduleFromLibrary } from "./policy-intelligence";
